services:
  plazafy-app:
    build:
      context: .
      args:
        # Provide the Gemini API Key at build time
        - GEMINI_API_KEY=${GEMINI_API_KEY}
    ports:
      - "8080:80"
    restart: always
